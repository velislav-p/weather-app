import React from 'react';

const Loader = (props) => {
    return (

        <div className="">
            <div className="loader">{props.message}</div>
        </div>

    )
};

export default Loader;